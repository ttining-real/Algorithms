/* -------------------------------------------------------------------------- */
/*                                    5명씩                                   */
/* -------------------------------------------------------------------------- */

const solution = (names) => names.filter((_, idx) => idx % 5 === 0);

console.log(
  solution(["nami", "ahri", "jayce", "garen", "ivern", "vex", "jinx"])
); // ["nami", "vex"]
